name: air_quality_dashboard
channels:
  - defaults
  - conda-forge
dependencies:
  - python==3.11
  - numpy
  - pandas
  - geopy
  - dash
  - plotly
  - ipykernel # only required for debugging
  - openpyxl
  - beautifulsoup4
  - lxml
  - pip # only required for debugging
  - pylint # only required for debugging
